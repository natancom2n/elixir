defmodule KitchenCalculator do

  ml = 1
  :cup = {1, 240}
  



  def get_volume(tupl), do: (tupl, 1)
end

# def compare(guess, guess), do: "Correct"
# def compare(secret, guess) when (guess - secret) in [1, -1], do: "So close"
